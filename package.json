{
  "name": "bs5-simple-starter",
  "version": "1.0.0",
  "description": "Simple workflow for working with and customizing Bootstrap 5",
  "main": "index.js",
  "scripts": {
    "sass:build": "sass --no-source-map scss:css",
    "sass:watch": "sass --no-source-map --watch scss:css"
  },
  "author": "Brad Traversy",
  "license": "MIT",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.4.0",
    "bootstrap": "^5.2.3",
    "sass": "^1.62.0"
  }
}
 {
  "name": "your-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'Build script placeholder'"
  },
  "dependencies": {
    "express": "^4.17.1"
    
  }
}
